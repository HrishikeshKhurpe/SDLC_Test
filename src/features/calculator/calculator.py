class Hrishikesh_Calculator:\n\n    def add(self, a, b):\n        \"\"\"Adds two numbers.\n\n        Args:\n            a (float): The first number to add.\n            b (float): The second number to add.\n\n        Returns:\n            float: The sum of the two numbers.\n        \"\"\"\n        try:\n            return a + b\n        except (TypeError, ValueError) as e:\n            raise ValueError(f\"Invalid input for addition: a={a}, b={b}\") from e\n\n    def subtract(self, a, b):\n        \"\"\"Subtracts two numbers.\n\n        Args:\n            a (float): The number to subtract from.\n            b (float): The number to subtract.\n\n        Returns:\n            float: The difference between the two numbers.\n        \"\"\"\n        try:\n            return a - b\n        except (TypeError, ValueError) as e:\n            raise ValueError(f\"Invalid input for subtraction: a={a}, b={b}\") from e\n\n    def multiply(self, a, b):\n        \"\"\"Multiplies two numbers.\n\n        Args:\n            a (float): The first number to multiply.\n            b (float): The second number to multiply.\n\n        Returns:\n            float: The product of the two numbers.\n        \"\"\"\n        try:\n            return a * b\n        except (TypeError, ValueError) as e:\n            raise ValueError(f\"Invalid input for multiplication: a={a}, b={b}\") from e\n\n    def divide(self, a, b):\n        \"\"\"Divides two numbers.\n\n        Args:\n            a (float): The number to divide.\n            b (float): The number to divide by.\n\n        Returns:\n            float: The quotient of the division.\n\n        Raises:\n            ZeroDivisionError: If the divisor (b) is zero.\n        \"\"\"\n        try:\n            return a / b\n        except ZeroDivisionError as e:\n            raise ZeroDivisionError(f\"Cannot divide {a} by zero\") from e\n        except (TypeError, ValueError) as e:\n            raise ValueError(f\"Invalid input for division: a={a}, b={b}\") from e